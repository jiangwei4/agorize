{% extends 'base.html.twig' %}

{% block title %}Hello {{ controller_name }}!{% endblock %}

{% block body %}
    <style>
        .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
        .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
    </style>

    <div class="example-wrapper">
        {{ form_start(form) }}
        <div class="w3-container w3-red">
            <h2>Input Form</h2>
        </div>
        <h2>All fields marked with an asterisk (<font color="red">*</font>) are mandatory</h2>
        <label class="w3-text"><b>{{ form_label(form.name) }}<font color="red"> *</font></b></label>
        <br> {{ form_errors(form.name) }}
        {{ form_widget(form.name) }}
        {{ form_help(form.name) }}
        <br>

        <br> <label class="w3-text"><b> {{ form_label(form.description) }}</b></label>
        <br>{{ form_errors(form.description) }}
        {{ form_widget(form.description) }}
        {{ form_help(form.description) }}
        <br>
        <label class="w3-text"><b>{{ form_label(form.user) }}<font color="red"> *</font></b></label>
        <br> {{ form_errors(form.user) }}
        {{ form_widget(form.user) }}
        {{ form_help(form.user) }}
        <br>

        {{ form_row(form.submit) }}

        {{ form_end(form) }}

    </div>






{% endblock %}
